{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npx prisma generate && cd apps/api && npx tsc -p tsconfig.json && cd ../.. && cp -r apps/api/dist _api_dist && node deploy-vercel.js",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "framework": null,
  "functions": {
    "api/index.js": {
      "memory": 1024,
      "maxDuration": 30,
      "includeFiles": "_api_dist/**,prisma/**,node_modules/.prisma/**,node_modules/@prisma/client/**"
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/pos/(.*)", "destination": "/pos/index.html" },
    { "source": "/menu/(.*)", "destination": "/menu/index.html" },
    { "source": "/((?!api|pos|menu).*)", "destination": "/index.html" }
  ]
}
